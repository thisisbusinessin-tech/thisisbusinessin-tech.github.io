<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tightly Stitched Containers — Wavy Seam</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      background-color: #e0e0e0;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      font-family: Arial, sans-serif;
    }
    /* Combined fabric layout */
    .stitched-container {
      width: 90%;
      max-width: 600px;
      background-color: #635049; 
      /* High-density fabric texture */
      background-image: 
        linear-gradient(rgba(255,255,255,.05) 1px, transparent 1px),
        linear-gradient(90deg, rgba(255,255,255,.05) 1px, transparent 1px);
      background-size: 3px 3px;
      box-shadow: 0 15px 35px rgba(0,0,0,0.3);
      border-radius: 6px;
      overflow: hidden;
      display: flex;
      flex-direction: column;
    }
    /* Showcase Boxes - Zero margin or gap between them */
    .box {
      padding: 40px;
      position: relative;
      color: #f5eae4;
      text-shadow: 1px 1px 1px rgba(0, 0, 0, 0.5);
    }
    /* Tightening the content spacing directly against the seam */
    .box-top {
      padding-bottom: 25px; 
    }
    
    .box-bottom {
      padding-top: 25px;
    }
    .box h2 {
      margin-top: 0;
      color: #ffffff;
    }

    /* THE REALISTIC SEAM & STITCH SYSTEM — now wavy instead of a straight line.
       Only this block changed: .seam-line is now just a sizing wrapper,
       everything visual (crease + double dashed thread) is one inline SVG
       so the whole seam can follow a wave path. Still zero images/JS. */
    .seam-line {
      width: 100%;
      height: 40px;
      position: relative;
      z-index: 10;
      line-height: 0; /* kills the tiny inline-svg baseline gap */
    }
    .seam-line svg {
      display: block;
      width: 100%;
      height: 100%;
    }
  </style>
</head>
<body>
  <div class="stitched-container">
    
    <!-- Top Container -->
    <div class="box box-top">
      <h2>Top Showcase</h2>
      <p>This panel tightly locks into the next one. Notice how the content sits closer to the seam to emphasize the pulled-together look.</p>
    </div>

    <!-- The Direct Seam — same crease + dashed thread as before, now on a wave -->
    <div class="seam-line" aria-hidden="true">
      <svg viewBox="0 0 600 40" preserveAspectRatio="none">
        <defs>
          <linearGradient id="creaseGrad" x1="0" y1="0" x2="0" y2="1">
            <stop offset="0%"  stop-color="rgba(0,0,0,0.2)"/>
            <stop offset="45%" stop-color="#1f1715"/>
            <stop offset="55%" stop-color="#120d0c"/>
            <stop offset="100%" stop-color="rgba(0,0,0,0.2)"/>
          </linearGradient>
        </defs>

        <!-- the dark pressed crease, following the wave, same thickness as the old 14px bar -->
        <path d="M0,20 Q150,34 300,20 T600,20"
              fill="none" stroke="#171010" stroke-width="14"
              stroke-dasharray="6 5" stroke-linecap="round" />

        <!-- top row of dashed thread, riding the same wave, offset up -->
        <path d="M0,14 Q150,28 300,14 T600,14"
              fill="none" stroke="rgba(255,255,255,0.85)" stroke-width="2"
              stroke-dasharray="6 5" stroke-linecap="round" />

        <!-- bottom row of dashed thread, offset down -->
        <path d="M0,26 Q150,40 300,26 T600,26"
              fill="none" stroke="rgba(255,255,255,0.85)" stroke-width="2"
              stroke-dasharray="6 5" stroke-linecap="round" />
      </svg>
    </div>

    <!-- Bottom Container -->
    <div class="box box-bottom">
      <h2>Bottom Showcase</h2>
      <p>This panel meets the top one exactly at the thread line. The shadow inside the seam makes the two layers look compressed together.</p>
    </div>
  </div>
</body>
</html>